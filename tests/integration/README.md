# tests/integration/

Jest integration tests exercising real Express routes against a test SQL
Server database (or a lightweight local instance), with Jira calls mocked
at the HTTP layer (e.g. `nock`).
