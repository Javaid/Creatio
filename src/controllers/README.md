# controllers/

Request handlers that translate HTTP requests into calls against
`services/`/`sync/`/`repositories/` and shape responses per
`docs/API-CONTRACT.md`. No business logic or direct DB access here.
