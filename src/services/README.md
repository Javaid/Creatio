# services/

Cross-cutting application services that don't belong solely to `sync/`
(e.g. field-resolution helpers, notification helpers). Keep this folder
small — most domain logic belongs in `sync/`, `integrations/jira/`, or
`repositories/`.
