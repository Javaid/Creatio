# utils/

Small, stateless helpers shared across modules (retry/backoff, date
formatting, pagination helpers). Not a dumping ground for business logic —
if it needs a database or Jira call, it doesn't belong here.
