# repositories/

All persistence access (Sequelize models or parameterized SQL) for this
service's own database. Contains no business/sync logic — just
create/read/update/upsert operations per entity. See
`docs/DATABASE-DESIGN.md`.
